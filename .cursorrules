【强制要求】每次对话开始前，必须先加载并阅读 @README.md 和 @REQUIREMENTS.md 两个文件的内容，结合这两个文件作为项目上下文基础，再按下方规则执行后续任务。

一、基础开发规则（强制）
沟通风格
跟用户聊天时尽量用大白话，通俗易懂，专业术语要解释清楚
代码注释也用通俗语言描述功能目的，不要写只有程序员才能理解的注释
技术栈固定
框架：原生微信小程序（WXML + WXSS + JS + JSON），禁止混用 uniapp 等第三方框架
样式：rpx 作为唯一单位，适配所有机型
兼容：支持微信基础库 2.10.0 及以上，覆盖 99% 用户
目录结构规范
plaintext
/pages        页面文件
/components   公共组件
/utils        工具函数（AI 请求、加密、格式化）
/config       配置文件（接口、AI 模型参数）
/images       静态资源
app.js/app.json/app.wxss 全局配置
代码规范
变量 / 函数：小驼峰 getAIResult
页面文件：英文小写 + 横线 ai-chat
注释：核心逻辑、AI 接口、异常处理必须加注释
禁止冗余代码、禁止硬编码密钥、禁止定时器滥用
二、AI 功能集成规则（核心）
AI 接口调用规则
统一封装 requestAI() 方法，所有页面统一调用
必须携带：加载状态 loading、失败重试、超时处理
请求参数脱敏：不传递用户隐私信息（手机号、位置等）
AI 能力范围
允许实现：
AI 问答 / 对话助手
AI 内容生成（文案、总结、推荐、解释）
AI 图片识别 / 分析
AI 表单智能填充
禁止实现：
AI 违法内容生成
AI 换脸、深度伪造
非授权用户数据训练
交互体验规则
AI 响应超时 >8s 必须提示：“AI 思考中，请稍候”
异常统一提示：“AI 服务异常，请重试”
历史对话自动缓存，不丢失
三、UI/UX 规则
风格统一
主题色：1 个主色 + 1 个辅助色，不超过 3 种主颜色
字体：统一使用微信默认字体，不引入外部字体
圆角统一：8rpx / 16rpx 二选一
AI 专属界面规范
对话气泡：左气泡（AI）、右气泡（用户）
复制、清空、重新生成按钮固定位置
长内容自动折叠，支持展开查看
禁止行为
禁止全屏广告、弹窗劫持
禁止遮挡微信导航栏、底部 tab
四、安全与隐私规则（微信审核必过）
密钥安全
AI 接口密钥、AppID 必须放在后端，禁止写在小程序前端代码
小程序只调用后端代理接口，不直接调用第三方 AI 平台
用户隐私
不收集非必要信息
不存储 AI 对话内容到本地，除非用户主动开启历史记录
禁止上传图片 / 内容到 AI 平台时泄露隐私
内容安全
AI 输出必须经过敏感词过滤
禁止返回政治、色情、暴力、医疗、法律误导性内容
五、微信平台合规规则（强制）
必须配置
app.json 正确配置页面路由、权限
app.js 完成微信登录、授权逻辑
必须添加 用户协议 + 隐私政策 页面
禁止行为
不允许诱导分享、诱导关注
不允许违规获取用户信息、手机号、位置
不允许支付功能未资质直接上线
AI 类小程序特殊要求
必须说明：“AI 生成内容仅供参考”
医疗 / 法律 / 金融类 AI 回答必须加免责声明
六、测试与上线规则
测试项
AI 接口正常 / 异常 / 超时场景
弱网、断网状态表现
安卓 /iOS 双端适配
微信审核敏感词测试
上线前检查
无控制台报错
无硬编码密钥
无隐私权限滥用
页面路径正确、无死链
极简版（直接复制给 AI 助手）
plaintext
AI 微信小程序开发规则：
1. 使用原生微信小程序开发，WXML+WXSS+JS+JSON。
2. 统一封装 AI 接口请求，带加载、异常、重试。
3. 样式用 rpx，适配全机型，界面简洁无广告。
4. 密钥放后端，前端不暴露任何密钥、不收集隐私。
5. AI 输出必须过滤敏感词，加“内容仅供参考”提示。
6. 必须包含用户协议、隐私政策页面。
7. 代码规范、结构清晰、注释完整，确保过微信审核。
